# Release Gate Artifacts

CI writes machine-readable compatibility gate summaries here before release artifact build/upload.

Generated summaries are runtime artifacts; keep this directory in version control so workflow paths remain stable.
