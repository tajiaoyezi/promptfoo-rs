# promptfoo-rs Compatibility Matrix

**Status**: Ready
**Baseline**: promptfoo 0.121.13 + commit 4860e99

Readiness basis: PRD §Compatibility Matrix, PRD §Compatibility Harness Design, ADR-006, ADR-007, ADR-009. Task 1.2 owns executable validation and §10 completion notes before Phase 1 can be Done.

| Capability | Level | Target Status | Verification | Owner | Notes / Gap Reason |
|---|---|---|---|---|---|
| CLI command/flag inventory | P0 | native | golden diff stdout/stderr/exit code | leafiellune | task 13.1 maps the command inventory and task 16.1 closes view/cache/import/export plus output/concurrency flag status as implemented local compatibility behavior |
| promptfooconfig.yaml/json | P0 | native | config normalization golden diff | leafiellune | vars, prompts, tests, providers, assertions |
| redteam.yaml | P0 | native | redteam fixture golden diff | leafiellune | init/generate/eval/run/report core flow covered by task 7.1 |
| .env and file prompts/tests | P0 | native | Linux/macOS/Windows path/env/newline fixtures | leafiellune | CSV/JSON/YAML tests included |
| Eval runner | P0 | native | mock provider result/error/metadata golden diff | leafiellune | task 13.2 covers output targets, assertion/provider failure exit codes, and resume metadata; latency normalized |
| Cache/resume/retry/concurrency/delay | P0 | native | cache key, resume cursor, partial result, retry fixtures | leafiellune | task 13.2 covers resume-from-cache for remaining cases; Azure/assistant special keys tracked as matrix children |
| OpenAI-compatible provider | P0 | native | request/response snapshot + golden diff | leafiellune | env/header/model/options coverage; task 14.1 verifies P0 fixture coverage through provider/assertion parity report |
| HTTP provider | P0 | native | request template/header/body/transform snapshot | leafiellune | common auth/header cases |
| Ollama provider | P0 | native | local mock server snapshot + golden diff | leafiellune | no real model download required |
| Anthropic provider | P0 | native | request/response snapshot + golden diff | leafiellune | network calls mocked |
| Other documented providers | P1/P2 | native/bridge/later | P1 request/output snapshot; P2 known gap row | leafiellune | P2 reason: task 14.1 keeps dynamic provider registry visible as P2/later with reason; no silent provider omission is allowed |
| Deterministic assertions | P0 | native | assertion result golden diff | leafiellune | equals/contains/regex/json/schema core assertions; task 14.1 verifies P0 assertion fixture coverage |
| Model-graded assertions | P1 | native/bridge | prompt, threshold, score parsing, metadata snapshot | leafiellune | P1 because true LLM output is non-deterministic; mock/recorded grader required |
| JS/TS custom provider/assertion | P0 | bridge | allow-scripts fixture, stdio/env/timeout/error snapshot | leafiellune | Shared sandbox default-deny, stdio, timeout, env allowlist, and redaction covered by task 9.1; task 14.1 records JS/TS boundary policy evidence |
| Python custom provider/assertion | P0 | bridge | subprocess fixture, stdio/env/timeout/error snapshot | leafiellune | Shared sandbox default-deny, stdio, timeout, env allowlist, and redaction covered by task 9.1; Python runtime discovery fixture remains follow-up |
| Shell/Ruby custom scripts | P1 | bridge | subprocess snapshot + security gate | leafiellune | Shell sandbox fixture covered by task 9.1; Ruby support depends on upstream 0.121.13 documentation inventory |
| JSON/JSONL/CSV/YAML output | P0 | native | schema + golden diff | leafiellune | JSONL result store streaming and SQLite query schema covered by task 5.1; JSON/JSONL/CSV/YAML formatter contract covered by task 5.2 |
| HTML/JUnit XML/SARIF output | P0/P1 | native | JUnit/SARIF schema snapshot; HTML data contract snapshot | leafiellune | JUnit/SARIF/HTML snapshots covered by task 5.2; SARIF finding production tied to scan phase |
| Local Web viewer | P1 | native web | result schema read/filter/export smoke | leafiellune | Task 10.1 covers JSONL/SQLite stable result schema loading, failed/provider/assertion table filtering, JSON/CSV export, and `promptfoo-rs.viewer.v1`; P1 because pixel-level upstream UI parity is out of scope |
| Compatibility harness / golden diff gate | P0 | native | baseline lock, upstream/rs artifact snapshot, normalization snapshot, release gate summary | leafiellune | Harness runner locks promptfoo@0.121.13 and normalization rules covered by task 6.1; release gate classification and P0/P1/P2 summary covered by task 6.2; task 10.2 binds gate evidence into release checklist and downgrades blocked stable releases to prerelease/nightly |
| Redteam plugins/strategies | P0/P1/P2 | native/later | full registry; core P0 golden diff; P1/P2 annotated | leafiellune | Core registry, risk score snapshot, and report artifact covered by task 7.2; task 14.2 adds item-level `RedteamInventoryCoverage`, P0 fixture count = 4, missing P0 fixture/blocker = 0; P2 reason required for long-tail plugins deferred after inventory |
| MCP provider / promptfoo mcp | P1 | native | protocol/request/response snapshot | leafiellune | Command skeleton, provider JSON-RPC snapshot, and target materialization error path covered by task 8.1; P1 until protocol coverage is complete |
| code-scans / scan-model / model-audit | P1 | native | CLI protocol, SARIF, finding schema snapshot | leafiellune | Task 8.2 covers `promptfoo-rs.scan.v1`, SARIF finding metadata, and `scan.false-positive-rate` as a known limitation; false positive rate is not a 1.0 gate |
| Node API wrapper | P1 | bridge | JS API contract snapshot and wrapper/core drift test | leafiellune | Task 9.2 covers thin TypeScript wrapper source, Rust JSON-RPC evaluate contract, `promptfoo-rs.node-api.v1`, and release-blocking `node-api-wrapper-drift`; npm package scaffold waits for Corepack-enabled packaging environment |
| promptfoo cloud/share | P2 | unsupported/later | capability registration, no-upload test, user-visible error | leafiellune | P2 reason: 1.0 explicitly does not provide SaaS or default upload behavior; brand/legal copy needs review before public release |

## Item-Level Matrix Artifact

Task 11.3 adds the machine-readable item-level matrix manifest at `compatibility/matrix/items.json`. The manifest expands from `compatibility/inventory/upstream-items.json` through `expand_matrix_from_inventory`, so every inventory item receives a row with level, target status, verification, owner, and fixture/snapshot/gap reference. Aggregate markdown rows remain human summaries only and cannot satisfy `validate_no_silent_omissions`.

Task 14.1 adds `validate_provider_assertion_parity` over the item-level matrix and fixture corpus. Current provider/assertion manual review: P0 provider count = 4, P0 assertion count = 6, P0 missing fixture/blocker count = 0, and P2 provider/assertion missing reason count = 0.

Task 17.1 adds frozen source-tree extraction evidence at `target/release-gates/source-extracted-items.json` and `target/release-gates/source-inventory-evidence.json`. The v2 evidence records promptfoo `0.121.13` / commit `4860e990c7e9a2f8f677173fb92cf9867b34d03f`, npm integrity, source counts, and release blockers for any source-extracted item not yet represented by the item-level matrix. Missing rows are intentionally preserved as blockers for task 17.4 classification rather than hidden from the matrix.

Task 18.1 adds `target/release-gates/source-inventory-ledger.json` and upgrades source inventory evidence from missing-row blocker accounting to explicit ledger accounting. Initial evidence recorded 2549 source-extracted items, 2549 ledger rows, 2116 generated accounting rows, `missing_matrix_rows=0`, `release_blockers=74`, and `p0_accounting_blocker_count=111`. This closes silent missing matrix rows; it does not claim those generated P0 rows are native parity.

Task 17.4 classifies the source-extracted provider/assertion/redteam long tail in tracked inventory and emits `target/release-gates/longtail-classification.json`. The initial artifact records 433 tracked long-tail rows, 0 missing tracked rows, 0 unresolved rows, 0 missing-reason rows, and 37 explicit P0 provider module release blockers that require dedicated fixture or blocker resolution before those per-file modules can be claimed as native parity.

Task 18.2 adds P0 provider module burndown evidence to `target/release-gates/longtail-classification.json`. Current evidence records `p0_provider_module_burndown.initial_blocker_count=37`, `resolved_by_fixture_count=13`, `remaining_blocker_count=24`, and `p0_release_blocker_count=24`; `p0_release_blockers[]` lists every remaining provider module blocker by item id, source reference, reason, verification, and external-authority flag. The 13 fixture-covered rows reuse existing P0 provider fixtures without real provider secrets; the remaining 24 rows stay release-blocking rather than being downgraded to P2/later.

Task 17.5 adds release installability evidence at `target/release-gates/installability.json` and includes it in runtime smoke / `release-candidate.json`. The current local evidence records installability_ready=`true`, publication_ready=`credential-blocked`, credential_blocked=`true`, channel-level published=`false`, release-candidate published=`false`, six local dry-run artifacts with checksums, 50 real P0 upstream corpus fixtures, and real upstream smoke exit code parity. This is not a claim that GitHub Releases, Homebrew, crates.io, Docker registry, or npm have been publicly published; those channels still require real credentials and release authority.

Task 18.4 adds publication authority evidence at `target/release-gates/publication-authority.json` and includes `publication_authority` in `target/release-gates/release-candidate.json`. Current evidence records publication_ready=`credential-blocked`, credential_blocked=true, legal_brand_blocked=true, six channel blockers, channel-level `installability_status` separated from `authority_status`, credential probes, legal/brand requirements, `published=false`, and `published_evidence=null`. This keeps local installability ready while preventing any stable public availability claim without real credentials, authorization, and external URL/digest evidence.

Task 19.1 reclassifies generated config rows whose source reference is under `promptfoo@0.121.13:src/app/**` as P1 Local Web viewer evidence. Current evidence records `viewer_config_reclassified_count=56`, `p0_accounting_blocker_count=55`, and `remaining_p0_blockers=55`. This is a viewer config scope correction, not a claim of complete upstream React UI parity: `src/app/**` config/editor/test files are accounted under the existing Local Web viewer P1 boundary, while non-app runtime config rows such as `commands/config.ts`, `configTypes.ts`, `globalConfig/*.ts`, and `util/config/*.ts` remain P0 blockers until task 19.2 adds fixture or blocker evidence.

Task 19.2 adds `core_config_source_burndown` to `target/release-gates/source-inventory-ledger.json` and `target/release-gates/source-inventory-evidence.json`. Current evidence records 18 non-app config source rows: 8 native/bridge fixture-covered runtime config rows, 7 explicit external cloud/server/telemetry blockers, 3 auxiliary P1 registrations, and `non_app_config_generic_blocker_count=0`. This reduces `p0_accounting_blocker_count` to 44 without counting external SaaS/server/telemetry configuration as local runtime parity.

Task 19.3 adds dedicated request/response fixtures for mockable remaining provider modules and keeps product-authority modules blocked. Current evidence records 22 fixture-covered provider modules, including 9 new dedicated request/response fixtures for Anthropic completion, HTTP multipart, and OpenAI completion/embedding/image/moderation/responses/transcription/video. The remaining 15 rows are external-authority modules such as Codex, Agents, Assistant, Billing, ChatKit, Realtime, and Claude Code auth; they remain release-blocking rather than being counted as complete.

Task 19.4 adds `target/release-gates/external-authority-blockers.json` as the combined external authority gate. Current evidence keeps the 15 provider product/account/credential blockers plus channel-level publication blockers visible with item ids, authority type, required decision, safe local fallback, and release impact. This gate records blocked or waived-with-boundary status only; it does not convert local mock evidence, dry-run installability, or publication authority notes into live product parity or public availability.

Task 20.1 adds cross-ledger provider source accounting reconciliation to `target/release-gates/source-inventory-evidence.json` and `target/release-gates/source-inventory-ledger.json`. Current evidence records 22 fixture-covered provider rows removed from source `remaining_p0_blockers`, 15 provider external-authority blockers preserved, 7 config external blockers preserved, `provider_source_generic_blocker_count=0`, and `p0_accounting_blocker_count=22`. This aligns source accounting with provider burndown while preserving that provider parity remains blocked by external product/account authority.

Task 20.2 adds `target/release-gates/perfect-refactor-claim.json` as the machine-readable perfect-refactor claim contract. Current evidence records `perfect_refactor_claim_allowed=false` while local stable release gates may report `stable_allowed=true`; blockers remain sourced from source accounting, current-upstream policy, external authority, and publication authority artifacts. This makes local stable readiness auditable without treating it as promptfoo perfect-refactor completion.

Task 21.1 adds `target/release-gates/upstream-distribution-target.json` as the distribution target disambiguation gate. The artifact records npm `promptfoo` core package metadata separately from GitHub repository HEAD and observed GitHub release tags. Current evidence can preserve frozen-baseline compatibility for the published npm core package when `npm_core_matches_frozen_baseline=true`, while still keeping repository-current perfect-refactor readiness blocked when `repository_head_matches_npm_core=false` or `github_latest_release_is_core_package=false`.

Task 22.1 adds `target/release-gates/perfect-refactor-unblock-packet.json` as the perfect-refactor blocker handoff artifact. It deduplicates provider blockers that appear in both source accounting and external authority artifacts, preserves source-only config blockers, and lists publication plus current-upstream rebaseline decisions with `auto_resolvable=false`. This packet makes the remaining external decisions explicit; it does not downgrade blockers or claim promptfoo perfect-refactor completion.

Task 23.1 updates `target/release-gates/upstream-distribution-target.json` to resolve the GitHub dynamic latest release ref from latest release metadata and record that ref in `github.source`. This prevents stale hard-coded release evidence while preserving the same matrix boundary: a non-core or mismatched latest release keeps `github_latest_release_is_core_package=false` and `current_repository_perfect_claim_allowed=false`, so the perfect-refactor blockers remain visible.

Task 24.2 adds the current-latest source inventory and matrix artifacts at `compatibility/inventory/current-latest-source-inventory.json`, `compatibility/matrix/current-latest-matrix.json`, `target/release-gates/current-latest-source-inventory.json`, and `target/release-gates/current-latest-matrix.json`. Current evidence is extracted from the locked GitHub default branch target in `compatibility/inventory/current-latest-target.json`, not the frozen 0.121.13 source inventory. Initial evidence records 3912 current-latest rows across command, flag, provider, assertion, redteam, output, config, viewer, Node API, example, docs, and unclassified categories; 318 unclassified rows keep `perfect_refactor_claim_allowed=false` until later tasks classify or implement them. This satisfies task 24.2 accounting closure only: it prevents silent omissions and does not claim full current-latest functional parity.

Task 24.3 adds `target/release-gates/current-latest-golden-corpus.json` and wires it into runtime smoke / `release-candidate.json`. Current evidence records 432 current-latest P0 fixture/artifact slots, 432/432 P0 artifact coverage, 2087/2087 P1 snapshot coverage, 1393/1393 P2 registration coverage, and 432 P0 release blockers. The corpus is intentionally larger than the frozen 50-fixture baseline and keeps `perfect_refactor_claim_allowed=false` while P0 rows remain blocked or unclassified.

Task 24.4 adds `target/release-gates/current-latest-quality.json` as the current-latest exhaustive quality gate. It aggregates adapter verification, source inventory, matrix, golden corpus, deterministic regression/stress/property checks, runtime smoke, external authority, publication authority, and claim wording into one machine-readable report. It permits only `no known release-blocking defects under declared gates` wording and keeps `perfect_refactor_claim_allowed=false` while current-latest blockers, external authority gaps, publication authority gaps, or impossible zero-bug wording remain.

Task 28.1 applies the task 19.3 / task 19.4 provider split to current-latest artifacts. Current evidence records 22 P0 provider rows as native fixture evidence, 16 P0 provider rows as explicit external-authority blockers, `current-latest-golden-corpus.blocker_count=70`, and `perfect_refactor_claim_allowed=false`. This reduces generic provider blockers without converting Codex, Agents, Assistant, Billing, ChatKit, Realtime, Claude Code auth, or other credential/product-authority gaps into local parity.

Task 29.1 applies the existing eval command, scheduler, retry, and output fixture evidence to current-latest eval-runner rows. Current phase-smoke evidence for target `96e556507e4bbee5110d94286d500c4605ccc38b` records 8 P0 eval-runner rows as native fixture evidence, 3 optimizer/event/synthesis rows as P1 snapshot evidence, 7 adaptive/rate-limit/provider-wrapper rows as explicit P0 blockers, `current-latest-golden-corpus.blocker_count=60`, and `perfect_refactor_claim_allowed=false`. The eval-runner group is reduced to 7 blockers; the total is 60 rather than the earlier 59 observation because the moving current-latest provider external-authority group is now 17. This reduces generic eval-runner blockers without claiming untested adaptive concurrency or provider rate-limit behavior as native parity.

Task 30.1 applies existing config/file prompt, eval prompt rendering, model-graded prompt, and script boundary evidence to current-latest prompt-processing rows. Live task §9 evidence for target `96e556507e4bbee5110d94286d500c4605ccc38b` records 4 P0 prompt index/string/text/utils rows as native fixture evidence, 3 constants/grading/Ragas helper rows as P1 snapshot evidence, 6 JSON/Markdown/Jinja/JavaScript/Python/executable processor rows as explicit P0 blockers, `current-latest-golden-corpus.blocker_count=53`, and `perfect_refactor_claim_allowed=false`. Final Phase 30 smoke uses deterministic release target fixtures derived from the tracked lock `1d09dfeb5f0766905409117f923dd5c4b0838d9f`, records the same prompt-processing 4/3/6 split, and records `blocker_count=52` with groups `cache-store=9, config=7, eval-runner=7, prompt-processing=6, provider=16, script-bridge=7`. This reduces generic prompt-processing blockers without claiming untested rich prompt processor or script execution behavior as native parity.

Task 31.1 applies existing cache key, resume, JSONL/SQLite result-store, local filesystem storage, and eval output cache evidence to current-latest cache-store rows. Task §9 evidence under the tracked current-latest target records 6 cache/database/storage rows as P0 native fixture evidence, 2 database testing/signal helper rows as P1 snapshot evidence, and `src/database/evalDeletion.ts` as the remaining explicit P0 cache-store blocker. `current-latest-golden-corpus.blocker_count=44`, groups are `cache-store=1, config=7, eval-runner=7, prompt-processing=6, provider=16, script-bridge=7`, `real-upstream-corpus.summary.status=ready` with 50 observed P0 fixtures, and `perfect_refactor_claim_allowed=false`. This reduces generic cache-store blockers without claiming deletion lifecycle semantics or cloud/publication authority.

Task 32.1 applies deterministic local config/eval prompt fixture evidence to current-latest JSON, Markdown, and Jinja prompt processor rows. Task §9 evidence under the tracked current-latest target records 7 prompt-processing rows as P0 native fixture evidence, 3 prompt-processing rows as P1 snapshot evidence, and JS/Python/executable processor rows as the remaining 3 explicit P0 script-bridge blockers. `current-latest-golden-corpus.blocker_count=41`, groups are `cache-store=1, config=7, eval-runner=7, prompt-processing=3, provider=16, script-bridge=7`, and `perfect_refactor_claim_allowed=false`. This reduces local prompt processor blockers without claiming script/subprocess processor parity or weakening script-bridge security boundaries.

Task 33.1 implements deterministic local SQLite eval deletion lifecycle evidence and applies it to the remaining current-latest cache-store blocker. Task §9 evidence under the tracked current-latest target records `cache-store:src-database-evaldeletion` as P0 native fixture evidence, cache-store blockers at zero, `current-latest-golden-corpus.blocker_count=40`, groups `config=7, eval-runner=7, prompt-processing=3, provider=16, script-bridge=7`, and `perfect_refactor_claim_allowed=false`. This removes the local cache-store blocker without changing remote/cloud delete command policy or external authority boundaries.

Task 34.1 implements deterministic local scheduler rate-limit/adaptive/provider-wrapper evidence and applies it to the seven remaining current-latest eval-runner blockers. Task §9 evidence under the tracked current-latest target records adaptive concurrency, provider rate-limit header parsing/state/key/registry, provider call execution context, and provider wrapper rows as P0 native fixture evidence, eval-runner blockers at zero, `current-latest-golden-corpus.blocker_count=33`, groups `config=7, prompt-processing=3, provider=16, script-bridge=7`, and `perfect_refactor_claim_allowed=false`. This removes local eval-runner scheduler blockers without treating local mock wrapper evidence as live provider or account-level rate-limit parity.

Task 35.1 implements deterministic local Node/Python subprocess evidence for JavaScript/Python/executable prompt processors and Python bridge wrapper/worker/pool rows. Task §9 evidence under the tracked current-latest target records prompt-processing blockers at zero, Python bridge rows as P0 native fixture evidence, Ruby bridge rows as the only remaining script-bridge blockers, `current-latest-golden-corpus.blocker_count=25`, groups `config=7, provider=16, script-bridge=2`, and `perfect_refactor_claim_allowed=false`. This removes local script prompt/Python bridge blockers without claiming Ruby runtime parity, external provider/config authority, publication, current-target readiness, or impossible zero-bug guarantees.

Task 36.1 implements deterministic local Ruby subprocess evidence for `src/ruby/rubyUtils.ts` and `src/ruby/wrapper.ts`. Task §9 evidence under the tracked current-latest target records Ruby bridge rows as P0 native fixture evidence, script-bridge blockers at zero, `current-latest-golden-corpus.blocker_count=23`, groups `config=7, provider=16`, and `perfect_refactor_claim_allowed=false`. This removes the last local script-bridge blockers without claiming config/provider external authority, publication authority, current-target readiness, or impossible zero-bug guarantees.

Task 37.1 refreshes `target/release-gates/perfect-refactor-unblock-packet.json` so current-latest artifacts are the authoritative handoff source when present. Task §9 evidence records `target_scope=current-latest`, `current_latest_golden_blocker_count=23`, `current_latest_external_authority_blocker_count=23`, `current_latest_required_decision_count=30`, groups `config=7, provider=16, current-latest target=1, publication=6`, and `perfect_refactor_claim_allowed=false`. This updates blocker accounting after Phase 36 without converting config/provider external authority, current-target, publication, or impossible zero-bug boundaries into local readiness.

Task 38.1 refreshes the tracked current-latest target lock after live upstream drift to `promptfoo@0.121.14`. Current lock evidence records npm gitHead `7a48c5fce614bee617efbb3b7fc93d404c75b628`, GitHub default branch HEAD `4d22e57f5f9b4c7cdde494f00558d9afde8b4975`, and GitHub latest release `refs/tags/0.121.14` / `7a48c5fce614bee617efbb3b7fc93d404c75b628`. The Rust and shell parsers now allow the npm tag and latest release to share one ref while recording both commit fields. `current_latest_claim_allowed=false` remains correct because downstream source inventory, matrix, golden corpus, quality, external authority, and publication evidence must all be refreshed or formally waived for the same target before any perfect-refactor claim can proceed.

Task 38.1 completion evidence records runtime-smoke output after the refresh: `current-latest-target.status=locked-with-drift`, `current_latest_claim_allowed=false`, current-latest golden corpus `fixture_case_count=94`, `p0_total=94`, `p1_total=2367`, `p2_total=1417`, `blocker_count=25`, current-latest quality `blocker_count=6`, release candidate `publication_ready=credential-blocked`, and upstream distribution target `status=blocked`. This confirms the refreshed target is consumed by downstream gates without weakening the compatibility matrix or claiming perfect refactor completion.

Task 39.1 classifies `promptfoo@0.121.14` source `src/evaluator/runtime.ts` as `eval-runner:src-evaluator-runtime` instead of `unclassified:src-evaluator-runtime`. The row remains `level=P0`, `implementation_status=blocked`, `verification_owner=eval-runner`, and `evidence_kind=blocker` until dedicated evaluator runtime fixture evidence exists. Runtime-smoke evidence after this task reports current-latest source inventory `status=ready`, matrix `status=ready`, both with `unclassified_rows=[]`, while current-latest quality remains `local_current_latest_ready=false` and `perfect_refactor_claim_allowed=false`.

Task 40.1 promotes `eval-runner:src-evaluator-runtime` to native fixture evidence: `level=P0`, `implementation_status=native`, `verification_owner=eval-runner`, `evidence_kind=fixture`, and `evidence_reference=fixture:eval-runner:src-evaluator-runtime`. Runtime-smoke evidence records evaluator runtime `release_blocker_count=0`, current-latest golden `blocker_count=24`, quality `blocker_count=4`, and `perfect_refactor_claim_allowed=false`. The remaining current-latest blockers are external config/provider authority, current-target, and publication authority decisions.

Task 41.1 refreshes the tracked current-latest GitHub repository target after default branch HEAD moved to `9d7d810c2118c63cb537bf05ea2d34c12bd22066` while npm latest and GitHub latest release remain `0.121.14` at `7a48c5fce614bee617efbb3b7fc93d404c75b628`. Runtime-smoke evidence records current-latest source inventory and matrix `status=ready` with `unclassified_rows=[]`, evaluator runtime fixture evidence still native, current-latest golden `blocker_count=24`, quality `blocker_count=4`, unblock packet `required_user_decision_count=31`, and `perfect_refactor_claim_allowed=false`. This is a target refresh only; it does not waive external authority, current-target, publication, or impossible bug-free claim blockers.

Task 42.1 refreshes the tracked current-latest GitHub repository target after default branch HEAD moved to `2ca16c59b64e0afca10533de0f817c0d24eba20a` while npm latest and GitHub latest release remain `0.121.14` at `7a48c5fce614bee617efbb3b7fc93d404c75b628`. Runtime-smoke evidence records current-latest source inventory and matrix `status=ready-with-blockers` with `unclassified_rows=[unclassified:src-evaluator-inmemorystore]`, evaluator runtime fixture evidence still native, current-latest golden `blocker_count=25`, quality `blocker_count=6`, unblock packet `required_user_decision_count=32`, and `perfect_refactor_claim_allowed=false`. The new `src/evaluator/inMemoryStore` row is surfaced explicitly rather than hidden. This is a target refresh only; it does not waive external authority, current-target, publication, or impossible bug-free claim blockers.

Task 46.1 classifies `promptfoo@current-latest:2ca16c59` source `src/evaluator/inMemoryStore.ts` as `eval-runner:src-evaluator-inmemorystore` instead of `unclassified:src-evaluator-inmemorystore`. The row remains `level=P0`, `implementation_status=blocked`, `verification_owner=eval-runner`, and `evidence_kind=blocker` until dedicated in-memory store fixture evidence exists. Runtime-smoke evidence after this task reports current-latest source inventory `status=ready`, matrix `status=ready`, both with `unclassified_rows=[]`, while current-latest quality remains `local_current_latest_ready=false` and `perfect_refactor_claim_allowed=false`.

Task 47.1 promotes `eval-runner:src-evaluator-inmemorystore` to `implementation_status=native`, `evidence_kind=fixture`, and `evidence_reference=fixture:eval-runner:src-evaluator-inmemorystore`. Runtime-smoke evidence after this task should reduce current-latest golden `blocker_count` by one for this local row while `local_current_latest_ready=false` and `perfect_refactor_claim_allowed=false` remain.

Phase 44 applies maintainer v1 policy in `docs/compatibility/v1-release-authority-policy.md`: `authority-decisions.json` resolves all 31 unblock-packet decision items via evidence (`current-latest:target`, `publication:github-releases`) or formal waivers (config, longtail providers, deferred publication channels). Evaluator in-memory store remains a local golden burndown item until task 47.1. `publication-evidence.json` keeps all six channels `published=false`; five channels document v1 deferral while GitHub Releases awaits the first tagged release URL and checksum. `perfect_refactor_decision_ready=true` on the authority gate does not imply `perfect_refactor_claim_allowed=true`.
